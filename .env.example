# fal.ai public Queue API. The VSN fork follows status_url/response_url returned by fal.
HF_API_BASE_URL=https://queue.fal.run

# Existing upstream upload path. Keep only if using Vercel Blob for reference media.
# VSN self-host phase 2 will replace this with our own object storage / upload adapter.
OPEN_HIGGSFIELD_READ_WRITE_TOKEN=
